# API keys — NEVER commit
.env
.env.local
.env.*.local

# Outputs
out/
out_*/
*.skill

# Python
__pycache__/
*.pyc
.pytest_cache/

# Caches (PMC XML cache for tools/build_apa_fixture.py 等)
.cache/

# Editor save backups (nano *.save, etc.)
*.save

# macOS Finder metadata
.DS_Store
**/.DS_Store

# uv virtual environment (Day27 で導入)
.venv/

# gitleaks history audit report (Day29 で導入、機密 finding を含み得るため commit しない)
.gitleaks-audit.json

# gitleaks smoke test の一時ディレクトリ (Day30 で導入、test 実行時のみ生成)
.gitleaks_smoke_tmp/
